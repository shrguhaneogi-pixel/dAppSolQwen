# Copy to .env.local. Leave NEXT_PUBLIC_PROGRAM_ID empty to run the simulation layer.
# After `anchor deploy`, run `anchor keys sync` and paste the printed id here.
NEXT_PUBLIC_PROGRAM_ID=
NEXT_PUBLIC_RPC_URL=https://api.devnet.solana.com
NEXT_PUBLIC_CLUSTER=devnet
